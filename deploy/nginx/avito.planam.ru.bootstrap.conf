server {
    listen 80;
    server_name avito.planam.ru;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        add_header Content-Type text/plain;
        return 200 "avito.planam.ru ACME bootstrap is ready\n";
    }
}
